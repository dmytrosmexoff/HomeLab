version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: homelab-url-short_app_1
      APP_PORT: 5656

  app:
    image: dmytrosmexoff/url-short:latest
    restart: on-failure
    stop_grace_period: 1m
    volumes:
      - ${APP_DATA_DIR}/data:/data
